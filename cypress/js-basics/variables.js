"Use strict";

let number = 5;
const number1 = 1000;

//camelCase (for variables)
//snake_case (for variables)
// UPPER_SNAKE_CASE (for constants)
// kebeb-case (for folders)
// PascalCase (for classes names)

//console.log(number);


// Прямых констант не бывает
const obj = {
    age: 20
}

// obj.age = 18;
obj = 1;
console.log(obj);

a = 1;